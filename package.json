{
  "name": "snapshot-study",
  "version": "1.0.0",
  "description": "",
  "main": "./out",
  "scripts": {
    "build": "npm run prepublish",
    "postinstall": "npm run prepublish",
    "prepublish": "babel lib --optional runtime --out-dir out --source-maps",
    "pretest": "npm run prepublish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bradleymeck",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.5.5",
    "heapdump": "^0.3.5",
    "v8-profiler": "^5.2.9"
  },
  "dependencies": {
    "babel-runtime": "^5.5.5"
  }
}
